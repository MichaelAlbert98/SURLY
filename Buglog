Implement catalog in similar fashion to other relations. (For each relation, store as multiple catalog entries where each entry contains info on one column of that relation's schema)
Change instances to constants. Make constant class that contains all constants. 